package Game;

/**
 * Simple settings container (educational).
 * Used by Menu to start server/clients with host/port and number of clients.
 */
public class GameSettings {
    public static String host = "127.0.0.1";
    public static int port = 5555;
    public static int clientCount = 1;
}
