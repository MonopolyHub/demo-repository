Build Rule Update
- Added real Monopoly rule:
  You can build houses/hotel ONLY if you own the full color group.
- Logic is enforced on SERVER (Single Source of Truth).
- UI automatically reflects allowed/denied builds via server responses.
